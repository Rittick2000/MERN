import Suva from "./Rittick"
function App() { // function name must always starts with capitale letter
  return ( // in jsx we can only return one element, that's why we wrap everything under <> </> (this is called fragment)
    <> 
    <h1>Rittick</h1>
    <Suva/>
    </>
  )
}

export default App
